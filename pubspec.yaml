name: my_finance
description: Personal finance management application

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  my_api:
    path: ../my_api
  hooks_riverpod:
  easy_localization:
  decimal:
  grouped_list:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/key/
    - assets/locale/